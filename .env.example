# Azure Cloud Resume Challenge - Environment Configuration

# Backend Configuration
# Copy this file to .env.local and fill in your actual values

# Visitor Counter API Configuration
# For production, set via Azure environment variables or Azure Key Vault
# DO NOT commit actual secrets - this file shows examples only

# Azure Storage/Cosmos DB Connection String
# Get this from Azure Portal > Storage Account > Access keys
# Format: DefaultEndpointsProtocol=https;AccountName=...;AccountKey=...;TableEndpoint=...
VISITOR_COUNTER_CONNECTION_STRING=your-cosmos-db-connection-string-here

# Visitor Counter API URL
# Used in frontend JavaScript - should be kept in a config file, not hardcoded
VISITOR_COUNTER_API_URL=https://your-function-app.azurewebsites.net/api/VisitorCounter?code=YOUR_FUNCTION_KEY

# Environment Name (development, staging, production)
ENVIRONMENT=development

# Enable Debug Logging
DEBUG_LOGGING=false
